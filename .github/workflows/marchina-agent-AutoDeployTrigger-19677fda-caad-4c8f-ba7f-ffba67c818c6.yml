name: Trigger auto deployment for marchina-agent

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/marchina-agent-AutoDeployTrigger-19677fda-caad-4c8f-ba7f-ffba67c818c6.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.MARCHINAAGENT_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.MARCHINAAGENT_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.MARCHINAAGENT_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: marchinaregistry.azurecr.io
          registryUsername: ${{ secrets.MARCHINAAGENT_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.MARCHINAAGENT_REGISTRY_PASSWORD }}
          containerAppName: marchina-agent
          resourceGroup: Java_1
          imageToBuild: marchinaregistry.azurecr.io/marchina-agent:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


